###############################################################
# Inputs
###############################################################

input_boolean:
  automated_lights:
    name: Automated lights
    icon: mdi:auto-fix

###############################################################
# Lights
###############################################################

light:
  - platform: group
    name: Groupe entree
    entities:
      - light.sonoff_entree_1
      - light.sonoff_entree_2
      - light.sonoff_entree_3

  - platform: group
    name: Toutes lumieres
    entities:
      - light.sonoff_entree_1
      - light.sonoff_entree_2
      - light.sonoff_entree_3
      - light.sonoff_chambre
      - light.sonoff_cleo
      - light.sonoff_couloir
      - light.sonoff_cuisine
      - light.sonoff_salle_de_bain
      - light.sonoff_salon
      - light.sonoff_sam
      - light.sonoff_shoes
      - light.sonoff_wc
      - light.sonoff_wc_wall

###############################################################
# Scripts
###############################################################

###############################################################
# Automation
###############################################################
automation:
  # ----- SaM et Salon

  - alias: Lumière salon
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_sam_action
        to: "single_right"
    action:
      - action: light.toggle
        entity_id: light.sonoff_salon

  - alias: Lumière salle à manger
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_sam_action
        to: "single_left"
    action:
      - action: light.toggle
        entity_id: light.sonoff_sam
  # ----- Cleo

  - alias: Lumière Cléo
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_cleo_action
        to: "single"
      - trigger: state
        entity_id: sensor.xiaomi_cleo_up_action
        to: "single"
    action:
      - action: light.toggle
        entity_id: light.sonoff_cleo

  - alias: Lumière Cléo indirecte
    triggers:
      - trigger: state
        entity_id: sensor.xiaomi_cleo_action
        to: "double"
      - trigger: state
        entity_id: sensor.xiaomi_cleo_up_action
        to: "double"
    action:
      - entity_id: switch.osram_cleo
        action: switch.toggle

      - entity_id: switch.osram_globe
        action: switch.toggle

  # ----- Chambre

  - alias: Lumière lit Parents
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_bedroom_action
        to: "single"
      - trigger: state
        entity_id: sensor.xiaomi_chambre_gauche_action
        to: "single"
      - trigger: state
        entity_id: sensor.xiaomi_chambre_droite_action
        to: "single"
    action:
      - action: light.toggle
        entity_id: light.sonoff_chambre

  - alias: Lumière lit Parents indirect
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_bedroom_action
        to: "double"
      - trigger: state
        entity_id: sensor.xiaomi_chambre_gauche_action
        to: "double"
      - trigger: state
        entity_id: sensor.xiaomi_chambre_droite_action
        to: "double"
      - trigger: state
        entity_id: sensor.xiaomi_shoes_action
        to: "double"
      - trigger: state
        entity_id: sensor.xiaomi_chambre_droite_action
        to: "hold"
      - trigger: state
        entity_id: sensor.xiaomi_chambre_gauche_action
        to: "hold"
    action:
      - entity_id: switch.osram_chambre
        action: switch.toggle

  # ----- Cuisine

  - alias: Auto Cuisine
    id: light_auto_cuisine
    use_blueprint:
      path: homeassistant/motion_light.yaml
      input:
        motion_sensor: binary_sensor.tuya_mmwave_presence_cuisine_presence
        switch_sensor: sensor.xiaomi_cuisine_action
        primary_light: light.sonoff_cuisine
        secondary_light: light.sonoff_cuisine
        light_sensor: sensor.illumination_7811dcfaa3ee
        min_lux: 1200
        no_motion_wait: 1
        no_action_wait: 1

  - alias: Auto Shoes
    id: light_auto_shoes
    use_blueprint:
      path: homeassistant/motion_light.yaml
      input:
        motion_sensor: binary_sensor.tuya_presence_shoes_presence
        switch_sensor: sensor.xiaomi_shoes_action
        primary_light: light.sonoff_shoes
        secondary_light: light.sonoff_shoes
        light_sensor: sensor.illumination_7811dcfaa3ee
        no_motion_wait: 1
        no_action_wait: 1

  # - alias: Lumière Cuisine
  #   trigger:
  #     - trigger: state
  #       entity_id: sensor.xiaomi_cuisine_action
  #       to: "single"
  #   condition:
  #     - condition: state
  #       entity_id: timer.cuisine_idle
  #       state: idle
  #   action:
  #     - service: light.toggle
  #       entity_id: light.sonoff_cuisine
  #     - service: timer.start
  #       target:
  #         entity_id: timer.cuisine_idle

  # - alias: Auto Cuisine
  #   use_blueprint:
  #     path: homeassistant/motion_light_on.yaml
  #     input:
  #       motion_sensor: binary_sensor.xiaomi_presence_cuisine_occupancy
  #       light: light.sonoff_cuisine
  #       light_sensor: sensor.xiaomi_presence_cuisine_illuminance
  #       timer: timer.cuisine_idle
  #       min_lux: 20

  # - alias: Auto Cuisine off
  #   use_blueprint:
  #     path: homeassistant/motion_light_off.yaml
  #     input:
  #       motion_sensor: binary_sensor.xiaomi_presence_cuisine_occupancy
  #       light: light.sonoff_cuisine
  #       timer: timer.cuisine_idle

  # - alias: Auto Cuisine 2
  #   use_blueprint:
  #     path: homeassistant/motion_light_on.yaml
  #     input:
  #       motion_sensor: binary_sensor.tuya_presence_cuisine_presence
  #       light_sensor: sensor.tuya_presence_cuisine_illuminance
  #       light: light.sonoff_cuisine
  #       timer: timer.cuisine_idle
  #       min_lux: 2000

  # - alias: Auto Cuisine off 2
  #   use_blueprint:
  #     path: homeassistant/motion_light_off.yaml
  #     input:
  #       motion_sensor: binary_sensor.tuya_presence_cuisine_presence
  #       light: light.sonoff_cuisine
  #       timer: timer.cuisine_idle

  # - alias: Auto Cuisine radar
  #   trigger:
  #     - trigger: state
  #       entity_id: binary_sensor.tuya_presence_cuisine_presence
  #   action:
  #     - service: light.turn_{{trigger.to_state.state}}
  #       entity_id: light.sonoff_cuisine

  # - alias: Lumière cuisine autoresync
  #   trigger:
  #     - platform: numeric_state
  #       entity_id: binary_sensor.xiaomi_presence_cuisine_occupancy
  #       attribute: illuminance
  #       above: 20
  #   condition:
  #     - condition: state
  #       entity_id: light.sonoff_cuisine
  #       state: "on"
  #   action:
  #     - service: light.turn_off
  #       entity_id: light.sonoff_cuisine

  # ----- Entrée

  - alias: Lumière entrée
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_entree_action
        to: "single"
      - trigger: state
        entity_id: sensor.xiaomi_entree_couloir_action
        to: "single_left"
    action:
      - action: light.toggle
        entity_id: light.sonoff_entree_1,light.sonoff_entree_2,light.sonoff_entree_3
      - action: timer.start
        data:
          entity_id: timer.entree

  - alias: Lumière entree auto off
    trigger:
      platform: event
      event_type: timer.finished
      event_data:
        entity_id: timer.entree
    action:
      action: light.turn_off
      data:
        entity_id: light.sonoff_entree_1,light.sonoff_entree_2,light.sonoff_entree_3

  - alias: Porte ouverte
    trigger:
      - trigger: state
        entity_id: binary_sensor.xiaomi_door_contact
        from: "off"
        to: "on"
    condition:
      - condition: state
        entity_id: alarm_control_panel.maison
        state: disarmed
    action:
      - action: light.turn_on
        data:
          entity_id: light.gateway_light_7811dcfaa3ee
          color_name: yellow
          brightness_pct: 100

  - alias: Porte fermée
    trigger:
      - trigger: state
        entity_id: binary_sensor.xiaomi_door_contact
        from: "on"
        to: "off"
    condition:
      - condition: state
        entity_id: alarm_control_panel.maison
        state: disarmed
    action:
      - action: light.turn_off
        data:
          entity_id: light.gateway_light_7811dcfaa3ee

  - alias: "Veilleuse alarme nuit"
    description: "Allume la lumière en bleu si elle change d'état alors que l'alarme est en mode nuit."
    trigger:
      - trigger: state
        entity_id: light.gateway_light_7811dcfaa3ee
        from: "on"
        to: "off"
    condition:
      - condition: state
        entity_id: alarm_control_panel.maison
        state: armed_night
    action:
      - action: light.turn_on
        data:
          entity_id: light.gateway_light_7811dcfaa3ee
          rgb_color: [0, 255, 255]
          brightness_pct: 25
    mode: single

  # ----- SDB

  - alias: Lumière salle de bain
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_sdb_action
        to: "single_left"
      - trigger: state
        entity_id: sensor.xiaomi_sdb_action
        to: "double_left"
    action:
      - action: light.toggle
        entity_id: light.sonoff_salle_de_bain

  # ----- Couloir
  - alias: Lumière couloir
    triggers:
      - trigger: state
        entity_id: sensor.xiaomi_couloir_action
        to: "single"
      - trigger: state
        entity_id: sensor.xiaomi_entree_couloir_action
        to: "single_right"
      - trigger: state
        entity_id: sensor.xiaomi_sdb_action
        to: "single_right"
    action:
      - action: light.toggle
        target:
          entity_id: light.tuya_light_rgb_couloir
        data:
          color_temp_kelvin: 6500
          brightness_pct: 100
      - action: timer.start
        data:
          entity_id: timer.couloir

  - alias: Lumière couloir permanent
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_couloir_action
        to: "double"
    action:
      - action: light.toggle
        target:
          entity_id: light.tuya_light_rgb_couloir
        data:
          color_temp_kelvin: 6500
          brightness_pct: 100

  - alias: Lumière couloir auto off
    trigger:
      trigger: event
      event_type: timer.finished
      event_data:
        entity_id: timer.couloir
    action:
      - action: light.turn_off
        entity_id: light.tuya_light_rgb_couloir

  # ----- Chaussures
  - alias: Lumière chaussures
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_shoes_action
        to: "single"
    action:
      - action: light.toggle
        target:
          entity_id: light.sonoff_shoes
      - action: timer.start
        data:
          entity_id: timer.chaussures

  - alias: Lumière chaussures auto off
    trigger:
      trigger: event
      event_type: timer.finished
      event_data:
        entity_id: timer.chaussures
    action:
      action: light.turn_off
      data:
        entity_id:
          - light.sonoff_shoes

  # ----- WC

  - alias: Lumière WC resync off
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_wc_action
        to: "hold"
    action:
      - action: light.turn_off
        entity_id: light.sonoff_wc_wall, light.sonoff_wc

  - alias: Auto WC radar
    use_blueprint:
      path: homeassistant/motion_light.yaml
      input:
        motion_sensor: binary_sensor.presence_wc_presence
        switch_sensor: sensor.xiaomi_wc_action
        primary_light: light.sonoff_wc
        secondary_light: light.sonoff_wc_wall
        light_sensor: sensor.illumination_7811dcfaa3ee
        no_motion_wait: 1
        day_start_hour: "08:00:00"
        night_start_hour: "22:00:00"

  # ----- All

  - alias: "Anti moustiques on"
    trigger:
      - trigger: state
        entity_id: alarm_control_panel.maison
        to: "armed_away"
    action:
      - action: switch.turn_on
        target:
          entity_id: switch.tuya_cuisine

  - alias: "Anti moustiques off"
    trigger:
      - trigger: state
        entity_id: alarm_control_panel.maison
        to: "disarmed"
    action:
      - action: switch.turn_off
        target:
          entity_id: switch.tuya_cuisine

  - alias: "Anti moustiques toggle"
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_cuisine_action
        to: "double"
    action:
      - action: switch.toggle
        target:
          entity_id: switch.tuya_cuisine

  # ----- Spare

  - alias: Test
    trigger:
      - trigger: state
        entity_id: sensor.xiaomi_spare_action
        to: "single"
    action:
      - action: light.toggle
        entity_id: light.sonoff_shoes

###############################################################
# Timer
###############################################################

timer:
  chaussures:
    duration: "00:05:00"
  couloir:
    duration: "00:05:00"
  entree:
    duration: "00:05:00"
