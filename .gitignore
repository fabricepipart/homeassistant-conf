# Root
*.db*
*.log
core*
*.conf
*user*
cloud
onboarding
person
notes
custom_components


# .storage
auth*
.core*
hacs*
alexa*

# Ignore folders.
.cloud
.google.token

# Ensure these YAML files are ignored, otherwise your secret data/credentials will leak.
ip_bans.yaml
secrets.yaml
known_devices.yaml